describe("Grid", function() {
  var grid;

  beforeEach(function() {
    grid = new Grid();
  });

  it("should create a new non empty grid with random numbers, given no state", function() {

  });

  it("should create a new non empty grid containing the state, given a state", function() {

  });

});
